class BaseballGame {
  constructor($target) {
    this.$target = $target;
    console.log(this);
  }

  play(computerInputNumbers, userInputNumbers) {
    return '결과 값 String';
  }
}

export default BaseballGame;
