import Util, { DIGITS } from "./util.js";

export default class BaseballGame {
  play(computerInputNumbers, userInputNumbers) {
    console.log(typeof computerInputNumbers, typeof userInputNumbers);
  }
}
